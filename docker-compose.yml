version: '3.8'

services:
  shouldisignthis:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - ./logs:/app/logs
      - ./contract_auditor.db:/app/contract_auditor.db
